{
  "compilerOptions": {
    "baseUrl": ".", // Sets the base path for resolving non-relative module names
    "paths": {
      "@/*": ["./src/*"] // Maps the @ alias to the src directory
    }
  }
}
